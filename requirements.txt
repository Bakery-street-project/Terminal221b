# Core dependencies
textual>=0.40.0
rich>=13.0.0
click>=8.0.0
pydantic>=2.0.0
tiktoken>=0.5.0
httpx>=0.25.0
toml>=0.10.0

# LLM providers (install as needed)
# openai>=1.0.0
# anthropic>=0.7.0

# Solana integration (Pro+ tier)
# solana>=0.30.0
# anchorpy>=0.18.0

# Local inference (requires CUDA)
# tensorrt-llm>=0.5.0
